<html>
<body>
<h1>${entity.name}</h1>
#foreach($field in $entity.attributes)
<div>
  <label for="${field.name}">${field.name}</label>
  <input type="text" name="${fn.uncapitalize($entity.name)}.${field.name}" id="${field.name}" />
</div>
#end
</body>
</html>
